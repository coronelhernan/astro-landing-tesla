---
import Section from "./Section.astro";

const {
  color = 'white', title = 'Solar Panels', data = 'Schedule a Virtual Consultation',
  buttons = {
    primary: {
      label: 'Order Now',
      href: '#'
    },
    secondary: {
      label: 'Learn More',
      href: '#'
    }
  }
} = Astro.props;
---

<Section
  color={color}
  id="section-solar-panel"
>
  <div slot="header" class="flex flex-col items-center gap-2">
    <h1 class="text-5xl font-semibold text-white">{title}</h1>
    <p class="text-white text-2xl font-semibold underline underline-offset-8">{data}</p>
  </div>

  <div slot="footer" class="gap-x-4 flex mx-auto">
    <a
      class="bg-custom-blue text-sm rounded font-medium text-white px-24 py-3 inline-block transition-opacity duration-300 hover:opacity-80"
      href={buttons.primary.href}>
      {buttons.primary.label}
    </a>

    <a
      class="bg-white text-sm rounded font-medium text-gray-800 px-24 py-3 inline-block transition-opacity duration-300 hover:opacity-80 hover:bg-gray-200"
      href={buttons.secondary.href}>
      {buttons.secondary.label}
    </a>
  </div>

  <img
    slot="background"
    alt="Model Solar Panel img"
    class="h-full w-full object-cover object-center"
    src="Solar-Panels-Img.avif"
    id="bg-image-models"
  />
</Section>